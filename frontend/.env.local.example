# Used by the browser via the Next.js rewrite (see next.config.js).
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Used only by Server Components when they fetch directly from inside Node.
# In Docker / split-deploy setups, this is the in-cluster URL.
API_BASE_INTERNAL=http://localhost:8000
